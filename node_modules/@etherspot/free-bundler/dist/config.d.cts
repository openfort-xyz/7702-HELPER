import { B as BundlerConfig } from './types-BWgm35W7.cjs';
import 'viem';
import 'viem/account-abstraction';

/**
 * Free bundler endpoints for different chains
 */
declare const FREE_BUNDLER_URLS: Record<number, string>;
/**
 * Chain configuration with metadata
 */
declare const BUNDLER_CONFIGS: Record<number, BundlerConfig>;
/**
 * Default transport options
 */
declare const DEFAULT_TRANSPORT_OPTIONS: {
    readonly timeout: 30000;
    readonly retryCount: 3;
    readonly retryDelay: 150;
};
/**
 * Default client options
 */
declare const DEFAULT_CLIENT_OPTIONS: {
    readonly name: "Free Bundler Client";
    readonly pollingInterval: 4000;
};

export { BUNDLER_CONFIGS, DEFAULT_CLIENT_OPTIONS, DEFAULT_TRANSPORT_OPTIONS, FREE_BUNDLER_URLS };
